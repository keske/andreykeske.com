import React from 'react';

export default () => (
  <div>
    <h1>Emoji From the Back</h1>
    <div>
      <img
        src={require('./files/emoji-back.png')}
        role="presentation"
      />
    </div>
  </div>
);
